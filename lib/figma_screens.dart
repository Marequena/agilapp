import 'package:flutter/material.dart';

// Auto-generated figma screens
final Map<String, WidgetBuilder> figmaRoutes = {
  '/adjuntos': (c) => const Screen_Adjuntos(),
  '/adjuntos': (c) => const Screen_Adjuntos(),
  '/aprobar_sala': (c) => const Screen_AprobarSala(),
  '/aprobar_vehiculo': (c) => const Screen_AprobarVehiculo(),
  '/calendario': (c) => const Screen_Calendario(),
  '/calendario': (c) => const Screen_Calendario(),
  '/detalle_pedido_cerrado': (c) => const Screen_DetallePedidoCerrado(),
  '/detalle_pedido_delay': (c) => const Screen_DetallePedidoDelay(),
  '/detalle_pedido_delay': (c) => const Screen_DetallePedidoDelay(),
  '/detalle_pedido_delay': (c) => const Screen_DetallePedidoDelay(),
  '/detalle_pedido_delay': (c) => const Screen_DetallePedidoDelay(),
  '/detalle_pedido_delay': (c) => const Screen_DetallePedidoDelay(),
  '/detalle_pedido_delay': (c) => const Screen_DetallePedidoDelay(),
  '/detalle_pedido_en_entrega': (c) => const Screen_DetallePedidoEnEntrega(),
  '/detalle_pedido_no_recibido': (c) => const Screen_DetallePedidoNoRecibido(),
  '/detalle_pedido_transporte': (c) => const Screen_DetallePedidoTransporte(),
  '/detalle_pedido_transporte': (c) => const Screen_DetallePedidoTransporte(),
  '/detalle_producto': (c) => const Screen_DetalleProducto(),
  '/detalle_solicitud_ant': (c) => const Screen_DetalleSolicitudAnt(),
  '/detalle_solicitud_pago': (c) => const Screen_DetalleSolicitudPago(),
  '/detalle_solicitud_sala': (c) => const Screen_DetalleSolicitudSala(),
  '/detalle_solicitud_vacaciones': (c) => const Screen_DetalleSolicitudVacaciones(),
  '/detalle_solicitud_vehiculo': (c) => const Screen_DetalleSolicitudVehiculo(),
  '/detalle_tarea': (c) => const Screen_DetalleTarea(),
  '/detalle_tarea': (c) => const Screen_DetalleTarea(),
  '/detalle_tarea': (c) => const Screen_DetalleTarea(),
  '/formulario_vacaciones': (c) => const Screen_FormularioVacaciones(),
  '/historial_asistencia': (c) => const Screen_HistorialAsistencia(),
  '/iconos_estado': (c) => const Screen_IconosEstado(),
  '/iconos_estado': (c) => const Screen_IconosEstado(),
  '/iconos_estado': (c) => const Screen_IconosEstado(),
  '/iconos_estado': (c) => const Screen_IconosEstado(),
  '/iconos_estado': (c) => const Screen_IconosEstado(),
  '/inicio_1': (c) => const Screen_Inicio1(),
  '/inicio_2': (c) => const Screen_Inicio2(),
  '/inicio_notificaciones': (c) => const Screen_InicioNotificaciones(),
  '/iphone_13_1': (c) => const Screen_Iphone131(),
  '/iphone_13_2': (c) => const Screen_Iphone132(),
  '/like_notch': (c) => const Screen_LikeNotch(),
  '/like_notch': (c) => const Screen_LikeNotch(),
  '/like_notch': (c) => const Screen_LikeNotch(),
  '/like_notch': (c) => const Screen_LikeNotch(),
  '/like_notch': (c) => const Screen_LikeNotch(),
  '/lista_solicitudes_ant': (c) => const Screen_ListaSolicitudesAnt(),
  '/lista_solicitudes_pago': (c) => const Screen_ListaSolicitudesPago(),
  '/login': (c) => const Screen_Login(),
  '/logo_google': (c) => const Screen_LogoGoogle(),
  '/mapa_envio': (c) => const Screen_MapaEnvio(),
  '/mis_pedidos': (c) => const Screen_MisPedidos(),
  '/notificaciones': (c) => const Screen_Notificaciones(),
  '/nueva_tarea': (c) => const Screen_NuevaTarea(),
  '/nueva_tarea': (c) => const Screen_NuevaTarea(),
  '/pedidos_entregados': (c) => const Screen_PedidosEntregados(),
  '/pedidos_pendientes': (c) => const Screen_PedidosPendientes(),
  '/pedidos_retrasados': (c) => const Screen_PedidosRetrasados(),
  '/pedidos_retrasados': (c) => const Screen_PedidosRetrasados(),
  '/registro_sala': (c) => const Screen_RegistroSala(),
  '/registro_vacaciones': (c) => const Screen_RegistroVacaciones(),
  '/registro_vacaciones': (c) => const Screen_RegistroVacaciones(),
  '/registro_vacaciones': (c) => const Screen_RegistroVacaciones(),
  '/registro_vehiculo': (c) => const Screen_RegistroVehiculo(),
  '/registro_vehiculo': (c) => const Screen_RegistroVehiculo(),
  '/registro_vehiculo': (c) => const Screen_RegistroVehiculo(),
  '/revisar_solicitudes': (c) => const Screen_RevisarSolicitudes(),
  '/sin_pedidos': (c) => const Screen_SinPedidos(),
  '/sin_pedidos_entrega': (c) => const Screen_SinPedidosEntrega(),
  '/tareas': (c) => const Screen_Tareas(),
  '/tareas_escritorio': (c) => const Screen_TareasEscritorio(),
  '/varios': (c) => const Screen_Varios(),
};

class Screen_Adjuntos extends StatelessWidget {
  const Screen_Adjuntos({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('adjuntos')),
      body: Center(
        child: Image.asset('assets/figma/adjuntos.png'),
      ),
    );
  }
}

class Screen_Adjuntos extends StatelessWidget {
  const Screen_Adjuntos({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('adjuntos')),
      body: Center(
        child: Image.asset('assets/figma/adjuntos.png'),
      ),
    );
  }
}

class Screen_AprobarSala extends StatelessWidget {
  const Screen_AprobarSala({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('aprobar_sala')),
      body: Center(
        child: Image.asset('assets/figma/aprobar_sala.png'),
      ),
    );
  }
}

class Screen_AprobarVehiculo extends StatelessWidget {
  const Screen_AprobarVehiculo({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('aprobar_vehiculo')),
      body: Center(
        child: Image.asset('assets/figma/aprobar_vehiculo.png'),
      ),
    );
  }
}

class Screen_Calendario extends StatelessWidget {
  const Screen_Calendario({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('calendario')),
      body: Center(
        child: Image.asset('assets/figma/calendario.png'),
      ),
    );
  }
}

class Screen_Calendario extends StatelessWidget {
  const Screen_Calendario({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('calendario')),
      body: Center(
        child: Image.asset('assets/figma/calendario.png'),
      ),
    );
  }
}

class Screen_DetallePedidoCerrado extends StatelessWidget {
  const Screen_DetallePedidoCerrado({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_pedido_cerrado')),
      body: Center(
        child: Image.asset('assets/figma/detalle_pedido_cerrado.png'),
      ),
    );
  }
}

class Screen_DetallePedidoDelay extends StatelessWidget {
  const Screen_DetallePedidoDelay({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_pedido_delay')),
      body: Center(
        child: Image.asset('assets/figma/detalle_pedido_delay.png'),
      ),
    );
  }
}

class Screen_DetallePedidoDelay extends StatelessWidget {
  const Screen_DetallePedidoDelay({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_pedido_delay')),
      body: Center(
        child: Image.asset('assets/figma/detalle_pedido_delay.png'),
      ),
    );
  }
}

class Screen_DetallePedidoDelay extends StatelessWidget {
  const Screen_DetallePedidoDelay({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_pedido_delay')),
      body: Center(
        child: Image.asset('assets/figma/detalle_pedido_delay.png'),
      ),
    );
  }
}

class Screen_DetallePedidoDelay extends StatelessWidget {
  const Screen_DetallePedidoDelay({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_pedido_delay')),
      body: Center(
        child: Image.asset('assets/figma/detalle_pedido_delay.png'),
      ),
    );
  }
}

class Screen_DetallePedidoDelay extends StatelessWidget {
  const Screen_DetallePedidoDelay({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_pedido_delay')),
      body: Center(
        child: Image.asset('assets/figma/detalle_pedido_delay.png'),
      ),
    );
  }
}

class Screen_DetallePedidoDelay extends StatelessWidget {
  const Screen_DetallePedidoDelay({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_pedido_delay')),
      body: Center(
        child: Image.asset('assets/figma/detalle_pedido_delay.png'),
      ),
    );
  }
}

class Screen_DetallePedidoEnEntrega extends StatelessWidget {
  const Screen_DetallePedidoEnEntrega({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_pedido_en_entrega')),
      body: Center(
        child: Image.asset('assets/figma/detalle_pedido_en_entrega.png'),
      ),
    );
  }
}

class Screen_DetallePedidoNoRecibido extends StatelessWidget {
  const Screen_DetallePedidoNoRecibido({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_pedido_no_recibido')),
      body: Center(
        child: Image.asset('assets/figma/detalle_pedido_no_recibido.png'),
      ),
    );
  }
}

class Screen_DetallePedidoTransporte extends StatelessWidget {
  const Screen_DetallePedidoTransporte({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_pedido_transporte')),
      body: Center(
        child: Image.asset('assets/figma/detalle_pedido_transporte.png'),
      ),
    );
  }
}

class Screen_DetallePedidoTransporte extends StatelessWidget {
  const Screen_DetallePedidoTransporte({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_pedido_transporte')),
      body: Center(
        child: Image.asset('assets/figma/detalle_pedido_transporte.png'),
      ),
    );
  }
}

class Screen_DetalleProducto extends StatelessWidget {
  const Screen_DetalleProducto({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_producto')),
      body: Center(
        child: Image.asset('assets/figma/detalle_producto.png'),
      ),
    );
  }
}

class Screen_DetalleSolicitudAnt extends StatelessWidget {
  const Screen_DetalleSolicitudAnt({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_solicitud_ant')),
      body: Center(
        child: Image.asset('assets/figma/detalle_solicitud_ant.png'),
      ),
    );
  }
}

class Screen_DetalleSolicitudPago extends StatelessWidget {
  const Screen_DetalleSolicitudPago({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_solicitud_pago')),
      body: Center(
        child: Image.asset('assets/figma/detalle_solicitud_pago.png'),
      ),
    );
  }
}

class Screen_DetalleSolicitudSala extends StatelessWidget {
  const Screen_DetalleSolicitudSala({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_solicitud_sala')),
      body: Center(
        child: Image.asset('assets/figma/detalle_solicitud_sala.png'),
      ),
    );
  }
}

class Screen_DetalleSolicitudVacaciones extends StatelessWidget {
  const Screen_DetalleSolicitudVacaciones({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_solicitud_vacaciones')),
      body: Center(
        child: Image.asset('assets/figma/detalle_solicitud_vacaciones.png'),
      ),
    );
  }
}

class Screen_DetalleSolicitudVehiculo extends StatelessWidget {
  const Screen_DetalleSolicitudVehiculo({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_solicitud_vehiculo')),
      body: Center(
        child: Image.asset('assets/figma/detalle_solicitud_vehiculo.png'),
      ),
    );
  }
}

class Screen_DetalleTarea extends StatelessWidget {
  const Screen_DetalleTarea({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_tarea')),
      body: Center(
        child: Image.asset('assets/figma/detalle_tarea.png'),
      ),
    );
  }
}

class Screen_DetalleTarea extends StatelessWidget {
  const Screen_DetalleTarea({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_tarea')),
      body: Center(
        child: Image.asset('assets/figma/detalle_tarea.png'),
      ),
    );
  }
}

class Screen_DetalleTarea extends StatelessWidget {
  const Screen_DetalleTarea({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('detalle_tarea')),
      body: Center(
        child: Image.asset('assets/figma/detalle_tarea.png'),
      ),
    );
  }
}

class Screen_FormularioVacaciones extends StatelessWidget {
  const Screen_FormularioVacaciones({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('formulario_vacaciones')),
      body: Center(
        child: Image.asset('assets/figma/formulario_vacaciones.png'),
      ),
    );
  }
}

class Screen_HistorialAsistencia extends StatelessWidget {
  const Screen_HistorialAsistencia({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('historial_asistencia')),
      body: Center(
        child: Image.asset('assets/figma/historial_asistencia.png'),
      ),
    );
  }
}

class Screen_IconosEstado extends StatelessWidget {
  const Screen_IconosEstado({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('iconos_estado')),
      body: Center(
        child: Image.asset('assets/figma/iconos_estado.png'),
      ),
    );
  }
}

class Screen_IconosEstado extends StatelessWidget {
  const Screen_IconosEstado({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('iconos_estado')),
      body: Center(
        child: Image.asset('assets/figma/iconos_estado.png'),
      ),
    );
  }
}

class Screen_IconosEstado extends StatelessWidget {
  const Screen_IconosEstado({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('iconos_estado')),
      body: Center(
        child: Image.asset('assets/figma/iconos_estado.png'),
      ),
    );
  }
}

class Screen_IconosEstado extends StatelessWidget {
  const Screen_IconosEstado({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('iconos_estado')),
      body: Center(
        child: Image.asset('assets/figma/iconos_estado.png'),
      ),
    );
  }
}

class Screen_IconosEstado extends StatelessWidget {
  const Screen_IconosEstado({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('iconos_estado')),
      body: Center(
        child: Image.asset('assets/figma/iconos_estado.png'),
      ),
    );
  }
}

class Screen_Inicio1 extends StatelessWidget {
  const Screen_Inicio1({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('inicio_1')),
      body: Center(
        child: Image.asset('assets/figma/inicio_1.png'),
      ),
    );
  }
}

class Screen_Inicio2 extends StatelessWidget {
  const Screen_Inicio2({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('inicio_2')),
      body: Center(
        child: Image.asset('assets/figma/inicio_2.png'),
      ),
    );
  }
}

class Screen_InicioNotificaciones extends StatelessWidget {
  const Screen_InicioNotificaciones({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('inicio_notificaciones')),
      body: Center(
        child: Image.asset('assets/figma/inicio_notificaciones.png'),
      ),
    );
  }
}

class Screen_Iphone131 extends StatelessWidget {
  const Screen_Iphone131({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('iphone_13_1')),
      body: Center(
        child: Image.asset('assets/figma/iphone_13_1.png'),
      ),
    );
  }
}

class Screen_Iphone132 extends StatelessWidget {
  const Screen_Iphone132({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('iphone_13_2')),
      body: Center(
        child: Image.asset('assets/figma/iphone_13_2.png'),
      ),
    );
  }
}

class Screen_LikeNotch extends StatelessWidget {
  const Screen_LikeNotch({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('like_notch')),
      body: Center(
        child: Image.asset('assets/figma/like_notch.png'),
      ),
    );
  }
}

class Screen_LikeNotch extends StatelessWidget {
  const Screen_LikeNotch({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('like_notch')),
      body: Center(
        child: Image.asset('assets/figma/like_notch.png'),
      ),
    );
  }
}

class Screen_LikeNotch extends StatelessWidget {
  const Screen_LikeNotch({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('like_notch')),
      body: Center(
        child: Image.asset('assets/figma/like_notch.png'),
      ),
    );
  }
}

class Screen_LikeNotch extends StatelessWidget {
  const Screen_LikeNotch({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('like_notch')),
      body: Center(
        child: Image.asset('assets/figma/like_notch.png'),
      ),
    );
  }
}

class Screen_LikeNotch extends StatelessWidget {
  const Screen_LikeNotch({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('like_notch')),
      body: Center(
        child: Image.asset('assets/figma/like_notch.png'),
      ),
    );
  }
}

class Screen_ListaSolicitudesAnt extends StatelessWidget {
  const Screen_ListaSolicitudesAnt({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('lista_solicitudes_ant')),
      body: Center(
        child: Image.asset('assets/figma/lista_solicitudes_ant.png'),
      ),
    );
  }
}

class Screen_ListaSolicitudesPago extends StatelessWidget {
  const Screen_ListaSolicitudesPago({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('lista_solicitudes_pago')),
      body: Center(
        child: Image.asset('assets/figma/lista_solicitudes_pago.png'),
      ),
    );
  }
}

class Screen_Login extends StatelessWidget {
  const Screen_Login({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('login')),
      body: Center(
        child: Image.asset('assets/figma/login.png'),
      ),
    );
  }
}

class Screen_LogoGoogle extends StatelessWidget {
  const Screen_LogoGoogle({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('logo_google')),
      body: Center(
        child: Image.asset('assets/figma/logo_google.png'),
      ),
    );
  }
}

class Screen_MapaEnvio extends StatelessWidget {
  const Screen_MapaEnvio({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('mapa_envio')),
      body: Center(
        child: Image.asset('assets/figma/mapa_envio.png'),
      ),
    );
  }
}

class Screen_MisPedidos extends StatelessWidget {
  const Screen_MisPedidos({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('mis_pedidos')),
      body: Center(
        child: Image.asset('assets/figma/mis_pedidos.png'),
      ),
    );
  }
}

class Screen_Notificaciones extends StatelessWidget {
  const Screen_Notificaciones({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('notificaciones')),
      body: Center(
        child: Image.asset('assets/figma/notificaciones.png'),
      ),
    );
  }
}

class Screen_NuevaTarea extends StatelessWidget {
  const Screen_NuevaTarea({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('nueva_tarea')),
      body: Center(
        child: Image.asset('assets/figma/nueva_tarea.png'),
      ),
    );
  }
}

class Screen_NuevaTarea extends StatelessWidget {
  const Screen_NuevaTarea({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('nueva_tarea')),
      body: Center(
        child: Image.asset('assets/figma/nueva_tarea.png'),
      ),
    );
  }
}

class Screen_PedidosEntregados extends StatelessWidget {
  const Screen_PedidosEntregados({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('pedidos_entregados')),
      body: Center(
        child: Image.asset('assets/figma/pedidos_entregados.png'),
      ),
    );
  }
}

class Screen_PedidosPendientes extends StatelessWidget {
  const Screen_PedidosPendientes({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('pedidos_pendientes')),
      body: Center(
        child: Image.asset('assets/figma/pedidos_pendientes.png'),
      ),
    );
  }
}

class Screen_PedidosRetrasados extends StatelessWidget {
  const Screen_PedidosRetrasados({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('pedidos_retrasados')),
      body: Center(
        child: Image.asset('assets/figma/pedidos_retrasados.png'),
      ),
    );
  }
}

class Screen_PedidosRetrasados extends StatelessWidget {
  const Screen_PedidosRetrasados({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('pedidos_retrasados')),
      body: Center(
        child: Image.asset('assets/figma/pedidos_retrasados.png'),
      ),
    );
  }
}

class Screen_RegistroSala extends StatelessWidget {
  const Screen_RegistroSala({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('registro_sala')),
      body: Center(
        child: Image.asset('assets/figma/registro_sala.png'),
      ),
    );
  }
}

class Screen_RegistroVacaciones extends StatelessWidget {
  const Screen_RegistroVacaciones({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('registro_vacaciones')),
      body: Center(
        child: Image.asset('assets/figma/registro_vacaciones.png'),
      ),
    );
  }
}

class Screen_RegistroVacaciones extends StatelessWidget {
  const Screen_RegistroVacaciones({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('registro_vacaciones')),
      body: Center(
        child: Image.asset('assets/figma/registro_vacaciones.png'),
      ),
    );
  }
}

class Screen_RegistroVacaciones extends StatelessWidget {
  const Screen_RegistroVacaciones({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('registro_vacaciones')),
      body: Center(
        child: Image.asset('assets/figma/registro_vacaciones.png'),
      ),
    );
  }
}

class Screen_RegistroVehiculo extends StatelessWidget {
  const Screen_RegistroVehiculo({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('registro_vehiculo')),
      body: Center(
        child: Image.asset('assets/figma/registro_vehiculo.png'),
      ),
    );
  }
}

class Screen_RegistroVehiculo extends StatelessWidget {
  const Screen_RegistroVehiculo({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('registro_vehiculo')),
      body: Center(
        child: Image.asset('assets/figma/registro_vehiculo.png'),
      ),
    );
  }
}

class Screen_RegistroVehiculo extends StatelessWidget {
  const Screen_RegistroVehiculo({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('registro_vehiculo')),
      body: Center(
        child: Image.asset('assets/figma/registro_vehiculo.png'),
      ),
    );
  }
}

class Screen_RevisarSolicitudes extends StatelessWidget {
  const Screen_RevisarSolicitudes({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('revisar_solicitudes')),
      body: Center(
        child: Image.asset('assets/figma/revisar_solicitudes.png'),
      ),
    );
  }
}

class Screen_SinPedidos extends StatelessWidget {
  const Screen_SinPedidos({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('sin_pedidos')),
      body: Center(
        child: Image.asset('assets/figma/sin_pedidos.png'),
      ),
    );
  }
}

class Screen_SinPedidosEntrega extends StatelessWidget {
  const Screen_SinPedidosEntrega({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('sin_pedidos_entrega')),
      body: Center(
        child: Image.asset('assets/figma/sin_pedidos_entrega.png'),
      ),
    );
  }
}

class Screen_Tareas extends StatelessWidget {
  const Screen_Tareas({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('tareas')),
      body: Center(
        child: Image.asset('assets/figma/tareas.png'),
      ),
    );
  }
}

class Screen_TareasEscritorio extends StatelessWidget {
  const Screen_TareasEscritorio({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('tareas_escritorio')),
      body: Center(
        child: Image.asset('assets/figma/tareas_escritorio.png'),
      ),
    );
  }
}

class Screen_Varios extends StatelessWidget {
  const Screen_Varios({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('varios')),
      body: Center(
        child: Image.asset('assets/figma/varios.png'),
      ),
    );
  }
}
